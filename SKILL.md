---
name: html-slide-skill
description: Use when asked to turn a decision-making report, technical manual, or long-form document into an HTML slide deck (rather than one long HTML page) — triggers on "슬라이드로 만들어줘", "보고서를 슬라이드로", "의사결정용 슬라이드", "HTML 슬라이드", "decision deck", "slide deck for review", or when a report/manual is dense enough that a single-page HTML would be hard to scan.
---

# HTML Slide Skill

의사결정 리포트·매뉴얼을 한 개의 긴 HTML 대신, 한 화면에 하나씩 넘겨보는 **HTML 슬라이드 덱**으로 만든다. 목적은 항상 하나: 독자가 문서를 완벽하게 이해하는 것. 길이 제한은 없다 — 이해에 필요하면 슬라이드를 더 쪼갠다.

## When to use

| 증상 | 판단 |
|---|---|
| 보고서/매뉴얼 내용이 많아 한 페이지 HTML로는 스크롤이 길고 가독성이 떨어짐 | 이 스킬 사용 |
| 발표/의사결정 회의에서 단계적으로 보여줘야 함 | 이 스킬 사용 |
| 표 1~2개, 짧은 안내문 수준의 가벼운 내용 | 과함 — 그냥 짧은 HTML 문서로 충분 |
| 발표자 노트, 인쇄용 PDF 내보내기 등 프레젠테이션 도구 기능이 필요 | `md-slides` 스킬(별도 markdown-html 파이프라인) 검토 |

## Non-negotiable rules

1. **항상 두괄식.** 덱·챕터·슬라이드·문단 모든 단위에서 결론을 먼저 쓰고 근거와 세부를 뒤에 둔다.
   - 1번 슬라이드: 결론/권고안을 한 문단으로 압축(본문 슬라이드보다 먼저).
   - 챕터 표지: 챕터 제목은 이름이어도 되지만, 바로 아래에 그 챕터의 결론 한 문장(`.chapter-lead`)을 목록보다 먼저 둔다.
   - 본문 슬라이드(한 장 요약 포함): 제목(`.title`)은 주제명이 아니라 그 슬라이드의 결론 한 문장이다. 본문은 그 결론의 근거·세부다. (나쁨: "실행 로그 화면" / 좋음: "실행 로그 화면 하나로 정상 여부를 판단합니다") 제목은 64px로 두 줄 이내.
   - 예외: 부록인 "출처"·"참고자료" 슬라이드 제목은 이름 그대로 둔다.
   - 문단·콜아웃·표 아래 설명: 첫 문장이 결론, 이어서 이유.
2. **챕터 구분.** 내용이 여러 주제로 나뉘면 챕터마다 표지 슬라이드를 넣는다. 챕터 표지는 본문 슬라이드와 **다른 배경**(예: 짙은 색 배경)을 쓰고, 그 챕터에서 다룰 내용을 요약한다.
3. **라이트 테마 고정.** 다크모드 토글 없음. 챕터 표지의 어두운 배경은 예외적 디자인 요소일 뿐, 문서 전체 테마 전환 기능은 만들지 않는다.
4. **이모지 금지.** 비즈니스 발표 톤을 유지한다.
5. **다이어그램은 Mermaid + 전체화면 버튼 필수.** 모든 Mermaid 다이어그램 카드에는 "전체화면" 버튼을 붙이고, 클릭 시 렌더링된 SVG를 확대된 모달로 보여준다.
6. **코드/CLI는 syntax highlight.** highlight.js로 하이라이트한다. 맨 텍스트나 이미지로 붙여넣지 않는다.
   - 대상: 사용자가 입력하거나 파일에 넣을 모든 코드·명령·설정. 여러 줄이면 `.code-card`, 화면에 보이는 값 하나(버전·경로)만 크게 보여 줄 때는 `.code-panel`을 쓴다.
   - 모든 `<pre><code>`에 언어 클래스를 붙인다(`language-bash`, `language-powershell`, `language-dos`, `language-sql`, `language-json`, `language-csharp` 등). 하이라이트가 의미 없는 실행 결과·화면 출력만 `nohighlight`.
   - Windows 명령(cmd)과 PowerShell은 공용 번들에 없어 템플릿이 `dos`·`powershell` 언어 파일을 따로 불러온다. 공용 번들에 없는 다른 언어를 쓰면 같은 방식으로 버전 고정 + SRI 해시와 함께 추가한다.
7. **표/차트/이미지를 적극 사용.** 텍스트로만 설명 가능한 내용도 표나 다이어그램으로 바꿀 수 있으면 바꾼다.
8. **항상 출처를 표기한다. 덱의 마지막은 출처 → 참고자료 순서의 부록이다.**
   - 본문의 사실·수치·인용(다이어그램·표 속 수치 포함)에는 출처 번호를 붙인다: `<a class="cite" href="#ref-1" data-ref="1">[1]</a>`. 번호를 누르면 출처 슬라이드의 해당 항목으로 이동하고, 브라우저 뒤로 가기로 돌아온다. 다이어그램 속 주장은 카드 아래 한 줄 설명에 번호를 붙인다.
   - "출처" 슬라이드 항목은 `<li id="ref-1">`처럼 번호와 id를 맞추고, 목록 번호도 `[1]`로 표시한다. 무엇을 뒷받침하는지(주장) + 자료명 + 발행처를 쓴다. 항목이 8개를 넘으면 출처 슬라이드를 여러 장으로 나눈다.
   - **인터넷 자료는 항상 전체 URL을 링크로 붙이고 확인일(YYYY-MM-DD)을 쓴다.** 이번 작업에서 실제로 열어 내용을 확인한 URL만 쓴다. 기억으로 URL을 만들어 넣지 않는다. 단축 URL, "웹 검색 결과", 사이트 이름만 쓰는 것은 출처가 아니다.
   - 사내 자료는 문서명과 파일 경로(또는 시스템 화면 이름), 사용자가 준 자료는 "사용자 제공 자료 — 파일명(또는 대화), 날짜"로 쓴다.
   - 출처를 댈 수 없는 수치는 넣지 않고 `[__]`로 비워 둔 뒤 사용자에게 알린다. 계산한 값은 "추정"이라고 밝히고 계산 방법과 원자료 출처 번호를 함께 쓴다.
   - "참고자료"는 본문에 직접 인용하진 않았지만 이해에 도움이 되는 배경 자료/용어다. 인터넷 자료면 여기도 URL과 확인일을 붙인다.
9. **디자인은 `references/design-system.md`를 따른다.** 슬라이드를 만들기 전에 이 문서를 반드시 읽는다. 색(남색 + 이스타항공 메인 컬러 `#D51F29`), 글자 크기 다섯 단계, 레이아웃 카탈로그, 금지 패턴이 들어 있다. `design-taste-frontend` 같은 다른 디자인 스킬은 여백·정렬 판단에만 참고하고, **색과 글꼴은 이 문서가 우선**한다.
10. **HTML을 만들거나 고칠 때마다 여러 에이전트로 검증하고, Red Team은 매번 반드시 포함한다.** 수정본도 다시 검증한다. 작성한 에이전트 자신은 검증자로 세지 않는다. 아래 "완성 후 검증" 참고.
11. **폰트와 최소 크기.** 제목은 IBM Plex Sans KR, 본문은 Noto Sans KR, 코드·경로·버전은 JetBrains Mono.
    - **화면에 실제로 보이는 글자는 9pt(12 CSS px) 이하가 되면 안 된다.** (브라우저 확대/축소 100% 기준)
    - 1920 캔버스 기준으로 24px 미만 글자를 쓰지 않는다. `<small>`, `<sup>`, 인라인 `font-size`로 줄이지 않는다. 템플릿은 캔버스를 0.55배 아래로 줄이지 않으므로(24px × 0.55 = 13.2px) 창이 작으면 축소 대신 스크롤된다. 이 배율 하한(`MIN_SCALE`)을 낮추지 않는다.
    - 템플릿이 열릴 때 24px 미만 글자와, 카드에 맞춰 줄어든 Mermaid 글자를 실제로 재서 콘솔에 경고한다. 경고가 나오면 글자를 키우지 말고 슬라이드나 다이어그램을 나눈다. 전체화면 모달도 같은 하한을 지킨다.
    - 스크린샷·이미지 속 글자도 같은 기준을 지킨다. 글자가 작은 이미지는 필요한 부분만 잘라 키우거나 표로 옮긴다.
    - 슬라이드 밖 탐색 막대 UI는 16px(12pt)로 고정돼 있다.
12. **페이지 이동.** 이전/다음 버튼과 번호 입력으로 이동하고, 새로고침해도 보던 장을 유지한다. 번호가 전체 장 수보다 크면 마지막 장, 1보다 작으면 첫 장으로 간다. (템플릿에 구현돼 있음: 하단 탐색 막대, 주소의 `#번호` + localStorage로 현재 장을 기억한다. 이 탐색 막대를 지우거나 숨기지 않는다.)

## Building the deck

`assets/template.html`을 복사해서 시작한다. 단일 파일 HTML(빌드 도구 없음)이며 이미 구현된 것:

- **1920×1080 고정 캔버스.** 창 크기에 맞춰 자동으로 확대·축소된다. `design-system.md`의 px 값을 그대로 쓴다.
- 슬라이드 이동: 화면 아래 탐색 막대(항상 보임, 슬라이드와 겹치지 않음)의 **[◀ 이전] [다음 ▶] 버튼**과 **번호 입력 + [이동] 버튼(또는 Enter)**. 방향키(←/→), PageUp/Down, Home/End도 동작한다. 범위를 벗어난 번호는 첫 장/마지막 장으로 보낸다.
- 새로고침 유지: 주소의 `#번호`로 기억하고, 주소에 번호가 없으면 localStorage에 저장된 마지막 장을 연다.
- 푸터 페이지 번호 자동 입력(`.foot-page`는 비워 둔다).
- 최소 글자 보장: 캔버스 배율 하한 0.55(그보다 작은 창에서는 스크롤), Mermaid 글자 크기 자동 점검.
- 레이아웃 16종 예시: 표지(결론 포함), 번호 3단 요약, 키-값 행, 챕터 표지, 흐름, 카드 3장, 번호 단계, 코드 패널, 표, Mermaid(+전체화면), 코드 블록, 강조 문장, 두 칸 비교, 한 장 요약, 출처, 참고자료.

만드는 순서:

1. `references/design-system.md`를 읽는다.
2. 슬라이드 제목만 먼저 순서대로 적어 이야기가 이어지는지 확인한다.
3. 슬라이드마다 레이아웃 카탈로그에서 하나를 고르고, 템플릿의 해당 `<section>`을 복사해 내용을 채운다. 쓰지 않는 예시 슬라이드는 지운다.
4. 레이아웃의 용량 한도를 넘으면 슬라이드를 나눈다.

`nav`, `progress`, 모달, 스크립트는 건드릴 필요 없음 — `document.querySelectorAll(".slide")` 기준으로 자동 동작한다. 새 스타일이 필요하면 인라인 스타일 대신 `<style>`에 클래스를 추가하고 CSS 토큰(`var(--accent)` 등)만 쓴다.

**주의 (실제로 겪은 버그):** 슬라이드를 `display:none`으로 숨기면 안 보이는 슬라이드의 Mermaid가 크기를 측정하지 못해 `translate(undefined, NaN)` 오류와 함께 다이어그램이 빈 카드로 남는다. 템플릿은 `opacity:0; pointer-events:none`으로 숨기는 방식을 이미 쓰고 있으니 이 구조를 유지한다.

전체화면 모달은 렌더링된 `<svg>`를 복제해서 키우는데, Mermaid가 svg에 박아두는 `style="max-width:...px"`부터 지워야 실제로 커진다 (`clone.style.maxWidth = "none"` 다음에 `width` 설정). 템플릿에 이미 반영돼 있다.

Mermaid는 `theme: "base"`와 `themeVariables`로 덱 색에 맞춰 두었다. 기본 테마로 되돌리지 않는다.

외부 리소스(Mermaid, highlight.js)는 CDN에서 **버전 고정 + SRI 해시**로 로드한다. Google Fonts CSS는 브라우저마다 응답이 달라 SRI를 걸 수 없는 유일한 예외다. 새 버전을 쓰려면 해시를 다시 계산해서 넣는다(하드코딩된 예전 해시를 그대로 복사하면 무결성 검증에 실패해 스크립트가 로드되지 않는다).

## 완성 후 검증

HTML을 만들거나 고칠 때마다, 사용자에게 넘기기 전에 **서브에이전트 3개 이상**으로 교차 검증한다. 생략하지 않는다.

| 역할 | 필수 | 보는 것 |
|---|---|---|
| **Red Team** | 매번 필수 | 문서를 깨뜨리려는 관점으로 비판한다. 숫자·단위 불일치, 과장되거나 근거 없는 결론, 출처 없는 수치, 출처와 본문 내용 불일치, 죽은 URL, 빠진 반론·위험을 공격적으로 찾는다. "문제 없음"으로 끝내지 말고 가장 약한 곳 3개를 반드시 지목한다. |
| 형식 | 필수 | 두괄식(모든 제목이 결론 문장인지), 챕터 구조, 출처 표기 형식, `references/design-system.md` 준수(24px·9pt 하한, 강조색 한 곳, 금지 패턴) |
| 내용/논거 | 필수 | 근거와 결론의 논리적 연결, 출처 번호가 실제 그 주장을 뒷받침하는지 |
| 표현 | 선택 | 어려운 용어, 이해하기 힘든 문장 |

- 각 에이전트에는 HTML 파일과 원본 자료를 함께 주고, 역할을 겹치지 않게 나눈다. 작은 수정(오타·색 한 곳)이어도 3개 역할을 모두 돌리되, 검토 범위를 바뀐 슬라이드로 좁힐 수 있다.
- 지적 사항은 모두 반영하거나, 반영하지 않는 이유를 적는다. 반영 후에는 Red Team을 다시 돌려 확인한다. 재확인은 최대 2회이며, 새로 나온 치명적 문제(사실 오류, 출처 없는 수치, 동작하지 않는 기능, 9pt 이하 글자)가 없으면 끝낸다. 남은 경미한 지적은 사용자 보고에 적는다.
- 네트워크가 없어 URL을 열어 볼 수 없으면, 확인하지 못한 URL 목록을 보고에 적는다.
- 사용자에게 넘길 때 검증 결과를 짧게 보고한다: 에이전트 구성, Red Team이 찾은 문제와 처리 결과, 남은 위험.
- 서브에이전트를 쓸 수 없는 환경이면, 역할별로 따로 검토 단계를 거치고(Red Team 단계 포함) **"서브에이전트 없이 자체 검토했다"고 사용자에게 분명히 밝힌다.** 자체 검토를 다중 에이전트 검증이라고 보고하지 않는다.

가능하면 Playwright 등으로 실제 브라우저에 열어 슬라이드 전환·번호 이동·새로고침 유지·Mermaid 렌더링(콘솔에 글자 크기 경고가 없는지)·전체화면 버튼·코드 하이라이트·출처 번호 이동이 실제로 동작하는지 최소 1회 확인한다.
